import React from "react";

const SearchBar = () => (
  <form>
    <input
      type="text"
      placeholder="&#xF002; Search"
      style={{ textAlign: "center" }}
    />
  </form>
);

export default SearchBar;
